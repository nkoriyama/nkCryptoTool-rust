# P2P scp パイプライン化レポート

`--scp-put`（アップロード）を「ファイルごとに ack を待つ直列送信」から「送信と ack 回収を
並行させるパイプライン送信」に変えた変更と、その効果の実測記録。

- 対象コミット: `perf(scp): pipeline the put path`（`cdf58ad1`）
- 直列版（比較対象）: 親コミット `799dc652`（`-r` 実装、ワイヤ互換・差はクライアントのみ）
- 計測日: 2026-07-02

## 1. 何を変えたか

**直列（変更前）**: クライアントは 1 ファイルを送るたびにサーバの `Ack` を待ってから次を送る。
ファイルごとに 1 往復（RTT）分の待ち時間が挟まる。

```
送信 file1 → [RTT 待ち] Ack1 → 送信 file2 → [RTT 待ち] Ack2 → …
```

**パイプライン（変更後）**: 送信と ack 回収を、双方向ストリームの独立した送受信半分を使う
2 つの並行タスク（`tokio::try_join!`）に分割する。送信タスクは全エントリ（`MkDir` /
`Put`+`Data`+`Eof`）を ack を待たずに流し `Done` で締め、回収タスクが `Ack`/`Fail` を並行して
読む。ack は単一の順序付きストリームで送信順に返るので、i 番目の ack が i 番目のエントリに
対応する（予約済みの `file_id` で確認）。

```
送信 file1 → file2 → file3 → … → Done   （フロー制御が in-flight を自動で抑える）
                回収 Ack1, Ack2, Ack3, …（並行）
```

- **新しい QUIC ストリームは開かない。カウンタ nonce 方式も不変**（＝暗号設計に手を入れない）。
  file_id 予約がそのまま活きる、最小侵襲の変更。
- サーバは無変更（元からストリーム処理＋エントリごと ack）。

## 2. 計測方法（公平な A/B）

- **同一サーバ**（OCI VPS 上の musl バイナリ）に対し、ローカルから**直列クライアントと
  パイプラインクライアントを当てる**。差はクライアントの送信スケジューリングのみ。
- 直列クライアントは親コミット `799dc652` を `git worktree` で建てたもの（ワイヤ互換）。
- 実 RTT が要る（loopback は RTT≈0 で差が出ない）ため VPS の実ネットワーク経由。
- ワークロード: 小ファイルのツリーを `--scp-put … -r`。ファイル数を振って傾向を見る。

> 補足: ローカルで制御遅延（netns + `tc netem`）による A/B も試みたが、rootless に必要な
> `unshare -r` がユーザ名前空間内で uid を 0 にマップし、`--serve-scp` の「root 実行拒否」
> ゲートに当たるため不可（セキュリティゲートは曲げない判断）。よって実 RTT の VPS を使用。

## 3. 結果

### 400 小ファイル `put -r`

| | 接続確立 | 転送分（400 ファイル） | 実時間合計 |
|---|---|---|---|
| **直列** | ~1.8s | **~1.8s**（≈ 4.5 ms/file = ack の RTT 往復） | **~3.6s** |
| **パイプライン** | ~1.8s | **~0.2s**（≈ 0.5 ms/file = サーバ処理律速） | **~2.0s** |

- **転送部分は約 9 倍高速、実時間で 3.6s → 2.0s（44% 短縮）。**
- 再現性: 直列 3.56 / 3.61s、パイプライン 2.02 / 1.98s（run 間のばらつき小）。

### 分解（固定コスト vs 転送コスト）

- **接続確立 ~1.8s**: 単一ファイル put もほぼ同じ時間 → 大半は接続確立（毎回 fresh endpoint を
  立てるホールパンチ + PQC ハンドシェイク + n0 discovery のコールドスタート）。両者共通・固定。
- **転送コスト**: 直列は 4.5 ms/file（ack の RTT 往復が支配）、パイプラインは 0.5 ms/file
  （RTT が隠れ、サーバの処理レートが律速）。この差 ≈ **1 ファイルあたり 1 RTT** がパイプライン化の
  削減分。

## 4. 学びと注意点

- **効果は「ファイル数 × RTT」に比例**。最初 100 ファイルで測ったときは差が見えなかった
  （転送分 ~0.45s が接続確立 ~1.8s に埋もれた）。**400 ファイルで顕在化**。ファイルが多いほど、
  また RTT が大きいほど効く。
- **loopback では no-op**（RTT≈0）。正しさ確認のみ（120 ファイルツリー往復一致）。
- **サーバは律速ではなかった**: パイプライン時 0.5 ms/file が出ている＝サーバの
  per-file コミット（fsync 含む）は RTT を隠せるだけ十分速い。直列の遅さは純粋に
  ack の往復待ち。
- **接続確立 ~1.8s は別軸の固定コスト**（コールドスタート）。多数ファイルを 1 接続で送る限り
  1 回で済むので、ファイルが多いほどパイプライン化の相対効果が大きくなる。

## 5. スコープと次段

- 本変更は **`put` 方向のみ**。`get` 方向（サーバが送信側）は依然エントリごとに ack 待ち＝
  同じ手法で並行化できるが未実施。
- **サーバ側コミットの並列化**（複数ファイルを同時に受信・fsync）は別物で、QUIC 複数ストリーム
  （`file_id` 多重化）が要る。nonce 分割（per-stream 鍵/nonce-prefix）が必須で中規模かつ
  慎重を要するため、実利（高速 LAN の bulk-many-files）が具体化するまで保留。詳細は
  `P2P_SCP_DESIGN.md`。

## 関連

- `P2P_SCP_DESIGN.md` — scp プロトコル設計、`-r` と並列化の方針。
- `P2P_INTEROP_EVIDENCE.md` §2.4 — scp オーバーヘッドの切り分け（本レポートの前提となる性能分析）。

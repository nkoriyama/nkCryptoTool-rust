# 信頼ブートストラップの設計思想

nkCryptoTool の P2P（shell / scp / chat）で「誰を信頼するか」をどう確立・保持するかの
設計判断と、その根拠。**現状の実装が既定として健全**であることと、一般向けに半歩寄せる場合の
選択肢・代償を地図として残す。**本書は rationale であり、TOEA 等の実装コミットではない。**

## 1. 思想: declarative / static config・監査可能

nkct の認可は徹底して **declarative**: confinement も policy も allowlist も
**「事前に配置した静的ファイルが真実の全て」**で、**実行時に信頼状態を書き換えない**。

- **攻撃面**: 実行時に信頼ストアを書き換えないので、**TOCTOU やストア汚染の攻撃面が原理的に
  存在しない**。allowlist は preload 後不変（`preload_allowlist` で起動時に読み、実行中は
  読むだけ）。
- **監査可能性**: 信頼の実体が人間可読な静的ファイルなので、`jj diff` で「いつ誰を信頼したか」が
  そのまま追える。合言葉を排し「明示・監査可能」を採る哲学と一貫。

## 2. 現状の信頼モデル

チケット（QR/`nkct1...` が運ぶもの）: **node ID（トランスポート識別）+ 到達アドレス（揮発）+
PQC 指紋（sign_fp / enc_fp、鍵由来で安定）**。

信頼は**方向で非対称**で、それが用途の非対称性（サーバ 1・クライアント複数）を反映している:

| 方向 | 何で確立するか | 状態 |
|---|---|---|
| **クライアント → サーバ（確認）** | チケット埋め込みの指紋を接続時に検証（`target_sign_fp`/`target_enc_fp`） | **実装済み・per-connection** |
| **サーバ → クライアント（認可）** | `--signing-pubkey <client_pub>` / `--peer-allowlist`（事前配置の静的ファイル） | **実装済み・静的・自動登録なし** |

- **永続化は無い**。TOFU / trust-store / known_hosts 相当のコードは存在しない。QR スキャンや
  接続の結果を信頼ストアに焼くステップが無い。「一度スキャンしたら以降信頼」にはならない。
- 「恒久ピンか一度きりか」の答えは **どちらでもなく "永続化しない（毎回設定を渡す）"**。

## 3. 設計空間 — 二元論ではなく多次元

ブートストラップは「TOFU vs 静的配置」の二択ではなく、少なくとも **3 軸**:

- **暗黙 / 明示**（信頼を自動で信じるか、人間が承認するか）
- **揮発 / 永続**（結果を残さないか、ストアに書くか）
- **監査可能 / 困難**（信頼変更を後から追えるか）

各手段の座標:

| 手段 | 暗黙/明示 | 揮発/永続 | 監査 | 備考 |
|---|---|---|---|---|
| **nkct 現状** | 明示 | 揮発 | 可 | 毎回設定を渡す。攻撃面ゼロ |
| SSH TOFU | 暗黙※ | 永続 | 困難 | ※指紋を**接続経路そのもの**で見せて yes → 初回 MITM に弱い |
| **display-only assist** | 明示 | 揮発 | 可 | ツールは指紋を QR/表示するだけ・人間がファイルに貼る |
| **TOEA** | 明示 | 永続 | 可 | 明示承認 → 既存 allowlist に追記 |

nct 現状は **「明示 × 揮発 × 監査可能」= 思想的に最も堅い隅**。SSH TOFU との差は承認プロセス
（暗黙→明示）だけでなく、**承認が依拠する情報の経路**にある（次節）。

## 4. QR は「独立チャネル」— TOEA が SSH TOFU より強い理由

対面 QR スキャンは、承認対象の指紋を**接続経路とは物理的に別のチャネル**（相手の画面→自分の
カメラ）で取得する。だから QR に基づく承認は**盲目的 TOFU ではなく OOB 認証済みピン留め**。

- SSH TOFU: 偽の指紋を接続経路で見せて yes を取れば MITM 成立。
- nkct + QR: MITM を消すのではなく、**「その QR が本物の相手の画面か」という人間が物理的に
  検証可能な一点に還元**する。それ以上は原理的に誰にも減らせない既約な最小前提。

## 5. UX 痛点の分解 — 転記ミス問題 ≠ 自動追記問題

元の痛点「fingerprint 手入力＝最悪の UX」は**2 つの直交した問題**:

1. **転記ミス（人的にエントロピーを落とす）** → **QR/表示で既に解ける**。`--qr` はその部品。
   人間は目視転記しない。
2. **ファイルに置く手間（ワンクリックで済ませたい）** → TOEA 自動追記が消すのはこれ**だけ**。

重要: **QR/表示で (1) を潰すのは declarative 思想を一切崩さない**（ツールは値を見せるだけ、
書くのは人間）。TOEA が追加で消すのは (2) のみで、その対価が実行時書き込み経路。
→ **技術者 / self-host 相手なら display-only assist で十分**。一般向けのワンクリックが要る
ときだけ TOEA。

## 6. 選択肢と代償（段階順）

- **(a) 現状維持**: 明示・揮発・監査可能。攻撃面ゼロ。技術者ユーザーには完全に機能。
- **(b) display-only assist**: 接続確立時に相手の指紋を QR/表示 → 人間が allowlist に貼る。
  **実行時書き込み経路ゼロ = 攻撃面を手放さない**。転記ミスだけ潰す。動くのは「ツールが書くか」
  ではなく「人間の手間」だけ。
- **(c) TOEA（Trust On Explicit Approval）**: 明示承認 → 既存 allowlist に追記。動くのは
  **揮発 → 永続の 1 軸だけ**（明示・監査可能は保つ）。ワンクリック UX。代償は実行時書き込み経路の
  新設。TOFU（暗黙・自動・ログ頼み）とは本質的に別物。

## 7. TOEA を実装する日の設計ノート

- **双方向は"対等な相互承認"ではなく"二役"**: 用途（サーバ 1・クライアント複数）に合わせ、
  「サーバの QR をクライアントがスキャン（確認）」＋「クライアントの指紋/QR をサーバオペレータが
  承認（allowlist 追記）」の役割が違う 2 ステップにする。**確認側は既に実装済み（チケット指紋）**
  なので、新規コードは**サーバ側の追記経路のみ**。
- **排他セレモニー = 並行性の根絶**: 信頼変更中はサーバが通常 listen を止め、承認だけ受ける専用
  状態に入る。承認と接続の競合を並行性ごと消す。「信頼の変更は通常運用と別の管理操作」として
  分離（SSH で `authorized_keys` を編集するのが運用と別行為なのと同じ）。
- **atomic 追記**: read → 新エントリ付加 → temp 全体書き → fsync → atomic rename。scp の staging と
  同手法で read-modify-write を不可分に。
- **approval-TOCTOU（表示値 ≠ 書込値）は排他では消えない・別対策**: 承認した値と書く値が**同一
  バイト**であること。このハンドシェイクで受け取った `client_dsa_pub` → その SHA3-256 → **同じ
  インメモリ値を表示し・同じ値を書く**（ファイル再読やパス間接を挟まない）。confinement を
  O_NOFOLLOW＋`/proc/self/fd` で潰したのと同じ「単一の信頼できるソースから離れない」原則を
  信頼ストア側にも適用する。**排他＝並行性、単一インメモリソース＝表示/書込整合、を別々に潰す。**

## 8. 結論

現状（明示・揮発・監査可能）は**既定として健全で、思想的に最も堅い隅**にいる。次の一歩の位置
（display-only assist → TOEA）と代償（(b) はゼロ、(c) は揮発軸のみ移動＋書き込み経路新設）も
見えている。**移るかどうかは「一般向けに広げる」がロードマップにあるか次第**で、無いなら
declarative のまま研ぎ澄ますのも完全に正しい。急ぐ話ではない。

## 関連

- `SECURITY_PROFILE.md` — 全体の脅威モデル。
- `P2P_SCP_DESIGN.md` / `P2P_SHELL_DESIGN.md` — confinement・policy・認可の実装。
- `P2P_SCP_COMPARISON.md` — QR チャネルを持つことの位置づけ。

# P2P シェル／到達手段の比較

NAT/CGNAT 配下のホストへ「踏み台なし・ポート開放なし」で到達し、対話シェルを
張る各手段の比較。

| ツール | P2P純度 | NAT越え | 暗号方式 | インストール必要 | 踏み台/中継 | 遅延 | 量子耐性 | 主な強み | 弱み | おすすめシチュエーション |
|---|---|---|---|---|---|---|---|---|---|---|
| **nkCryptoTool（yours）** | ★★★★★ | Iroh（ホールパンチ） | Hybrid PQC + AES-256-GCM ※1 | バイナリ1つ | 自己ホスト Relay 可 | 低〜中 | ◎ | 量子耐性・純 P2P・軽量 | まだ開発中 ※3 | 安全性最優先、Tailscale 使えない環境 |
| Tailscale SSH | ★★★★ | 優秀 | WireGuard | クライアント必須 | あり | 非常に低 | △ | 簡単・安定・企業向け | クライアント必須、Tailnet 依存 ※3 | 日常使い、複数台管理 |
| ZeroTier | ★★★★ | 良好 | 独自 | クライアント必須 | あり | 低 | △ | 仮想 LAN 簡単 | 中央管理要素あり | シンプル VPN 代替 |
| Holesail | ★★★★★ ※4 | 優秀 | Noise (Curve25519) ※4 | 要 Node.js ※4 | なし | 低 | △ | 完全サーバーレス・簡単 | 対話シェルなし（トンネル経由で SSH を流す）※4 | ポート公開・一時的接続 |
| mosh | ★★ | − | SSH ベース | 両側必要 | − | 非常に低 | △ | 回線不安定に強い | NAT 越え弱い、P2P ではない | 移動中・不安定回線 |
| SSH（標準） | ★ | なし | PQC KEX＋古典署名 ※2 | 標準 | 踏み台必要 | 低 | ○ ※2 | 互換性最強 | ポート開放・NAT 問題 | シンプル環境 |

## 脚注

- **※1 — nkCryptoTool の「PQC + AEAD」の内訳**: ライブチャネル鍵は
  **P-256 (prime256v1) ECDH ‖ ML-KEM-768 ハイブリッド**（IKM = `ss_ecc ‖ kem_ss`、
  salt = SHA3-256(ハンドシェイク transcript) を噛ませた HKDF-SHA3-256 で導出、
  ドメイン分離 `nk-auth-v3`）。transcript に KEM 暗号文と両公開鍵が含まれ、
  暗号文バインドが成立する（**設計意図としては**、攻撃者は P-256 と ML-KEM-768 の
  双方を破らない限り鍵を復元できない。ただしこの堅牢性は下記のとおり標準 combiner と
  異なり未形式検証）。認証は **純 ML-DSA-65 署名**（KEM とは独立、指紋ピンニング）、
  AEAD は **AES-256-GCM**。結合関数は X-Wing / Chempat 等の標準（形式検証済み）
  構成ではなく、transcript バインドに基づく独自構成。salt 経由のバインドでも
  導出鍵が暗号文に依存するため**バインドの効果としては IKM バインドと同等**だが、
  形式検証済み combiner ではない点は留保。
  ※非対称設計: 秘匿は古典＋PQC のハイブリッド（保険つき）、認証は純 ML-DSA。
  harvest-now-decrypt-later に対しては秘匿を二重化し、認証はリアルタイム性ゆえ
  純 PQC で簡潔に保つ、という脅威モデル整合の選択。

- **※2 — SSH の量子耐性**: OpenSSH は **10.0（2025年4月）以降、鍵交換が
  `mlkem768x25519-sha256`（ML-KEM ハイブリッド）でデフォルト PQC**（9.0 以降は
  `sntrup761x25519` も提供、10.1 からは非 PQC 接続に警告）。よって**秘匿（KEX）は
  PQC 化済み**で、PQC が全く無い Tailscale/WireGuard（△）より上位（○）。ただし
  **ホスト鍵・認証署名は依然古典**（Ed25519/RSA）であり、これが暗号方式欄の
  「古典署名」が指すところ（KEX は PQC・署名は古典、という分離を表記に反映している）。したがって SSH は「**秘匿 PQC・認証古典**」で、KEX も認証も PQC の
  nkCryptoTool（◎ ＝ エンドツーエンド full PQC）とは区別される。なお旧バージョン
  （〜8.x）の SSH には PQC KEX が無い。

- **※3 — 第三者観測点の補足**: nkCryptoTool は到達のためのディスカバリが
  **チケットのみ**（DNS/DHT 等のサードパーティ解決なし。`presets::Minimal` ＋
  `DiscoveryMode::None` の帰結で、プレゼンスも公開しない）。一方 relay は
  **既定では n0 の公開リレーを使う**（接続メタデータ＝両端の所在・タイミングは
  リレー運用者から見えるが、ライブチャネルは E2E 暗号でリレーに平文は渡らない）。
  relay は `--relay-url` で自己ホストでき、直接ホールパンチ成立時は relay 自体が
  不要。よって**到達のために必須の第三者は、既定では中継層（n0 リレー）に限られ、
  自己ホスト relay か直結成立の構成では完全ゼロにできる**（発見層は既定でゼロ、
  制御プレーンはチケットベース P2P ゆえ構造的に不在）。Tailscale SSH は標準構成で coordination と
  DERP が Tailscale 社依存（Headscale は coordination を自己ホスト化する代替）。
  ※トレードオフ: 制御プレーン不在の代償として、チケットを帯域外の安全経路で
  事前共有する必要がある（座標・到達候補を埋め込むぶんチケットは長く、
  `direct_addrs` 変化で短命になりやすい）。SSH の `known_hosts` 初回信頼問題を
  帯域外チケット配布で置き換えた形。

- **※4 — Holesail の補足**: hyperdht（Hypercore DHT）上の P2P トンネルで、
  **単一バイナリではなく Node.js ランタイムを要する**。暗号化は Noise 系（古典
  Curve25519 ＝ PQC なし）。**ピア発見は DHT に依存し、その既定ブートストラップは
  3つの公開ノード**（`node{1,2,3}.hyperdht.org:49737`、"on behalf of the commons"
  として公開運用）であり、発見層に第三者が**既定で**介在する。hyperdht 自体は
  隔離 DHT（`bootstrap:[]` ＋ 自前ブートストラップ＋永続ノード）で自己ホスト可能だが、
  公開 IP・ポート開放のノードを立て、永続ノードの安定に約30分を要するなど、
  nkCryptoTool の「**発見インフラ自体が不要**（チケット直結）」より重い
  （Holesail CLI が bootstrap 上書きを露出するかは未確認）。対話シェルは持たず、
  TCP トンネル経由で別途 SSH を流す構成。よって両者 ★★★★★ でも「第三者観測点ゼロ」の
  観点では質が異なる（nkCryptoTool = 既定でゼロ／Holesail = 既定で第三者、
  自己ホストは DHT インフラ構築が必要）。

---

### 凡例

- **P2P純度**（評価軸）: 到達・制御・中継を第三者インフラへ依存しない度合い。
  ★が多いほど自己完結（最良ケースで第三者ゼロに近い）。発見/中継/制御のどの層に
  第三者が残るかは「第三者観測点」の項を参照（★が同数でも既定挙動で質が異なる）。
- **量子耐性**: ◎ ＝ 秘匿・認証ともに PQC（エンドツーエンド）／○ ＝ 秘匿（KEX）は
  PQC・認証署名は古典／△ ＝ PQC なし（古典のみ）
- **第三者観測点**（到達に必須の外部依存）: 発見・中継・制御の各層に第三者が
  残るかで質が異なる。nkCryptoTool は構成次第で全層ゼロ可（※3）、SSH は踏み台が
  中継層の観測点、Tailscale は制御層が Tailscale 社依存。
